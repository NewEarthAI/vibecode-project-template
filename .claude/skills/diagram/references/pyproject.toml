[project]
name = "excalidraw-render"
version = "1.0.0"
description = "Playwright-based Excalidraw diagram renderer for visual validation"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
]
