{
  "name": "topology-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local in-repo viewer for the Intent-Actual-Gap drift map (Wave 1). Reads graph.json + drift-overlay.json and renders a drift-first interactive topology. Render-target-agnostic — the same two JSONs feed any future surface (Agency OS, Hermes).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@xyflow/react": "^12.3.0",
    "elkjs": "^0.9.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
